# Notification Icons

64x64 PNG icons used by the ntfy notification system.
Served via `raw.githubusercontent.com` URLs.

See the icon inventory in `skills/vault-triage/SKILL.md` for the full list
and per-agent/per-status mapping.

Replace placeholder PNGs with real 64x64 icons before enabling icon support.
