bash:
  # curl (for fetching advisory databases, documentation)
  "curl*": allow
  # Rust static analysis + coverage
  "cargo clippy*": allow
  "cargo audit*": allow
  "cargo deny*": allow
  "cargo llvm-cov*": allow
  "cargo test*": allow
  # Node static analysis + coverage
  "npm audit*": allow
  "pnpm audit*": allow
  "yarn audit*": allow
  "eslint*": allow
  "tsc*": allow
  "jest*": allow
  "vitest*": allow
  "npx jest*": allow
  "npx vitest*": allow
  # Python static analysis + coverage
  "pip-audit*": allow
  "safety*": allow
  "ruff*": allow
  "mypy*": allow
  "bandit*": allow
  "pytest*": allow
  # Cross-language
  "semgrep*": allow
  "trivy*": allow
