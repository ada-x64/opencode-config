bash:
  # Test/validation suite (read-only observation, no build mutations)
  "cargo test*": allow
  "cargo clippy*": allow
  "pytest*": allow
  "jest*": allow
  "vitest*": allow
  "npx jest*": allow
  "npx vitest*": allow
  "tsc*": allow
