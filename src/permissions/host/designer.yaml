bash:
  # Vault write (filesystem)
  "mv *": allow
  "rm *": allow
  "mkdir *": allow
  "cp *": allow
  "ln *": allow
  "mktemp*": allow
  "rmdir *": allow
  "chmod *": allow
  "touch *": allow
  "install *": allow
