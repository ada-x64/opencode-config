# Notification Icons

PNG icons used by the ntfy notification system.
Served via `raw.githubusercontent.com` URLs.

Current files are 1×1 transparent placeholders. Replace with real 64×64 icons
before enabling icon support in production.

See the icon inventory in `skills/vault-triage/SKILL.md` for the full list
and per-agent/per-status mapping.
